overwrite: true
hooks:
  afterAllFileWrite:
    - prettier --write
schema:
  - ./schema.graphql
generates:
  packages/tools/src/generated.ts:
    plugins:
      - typescript
      - typescript-operations
    documents:
      - ./packages/tools/src/**/*.ts*
